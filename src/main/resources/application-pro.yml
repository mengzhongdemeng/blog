spring:
  datasource:
    password: root
    username: root
    url: jdbc:mysql://localhost:3306/blog?useUnicode=true&characterEncoding=utf-8
    driver-class-name: com.mysql.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true
logging:
  level:
    root: warn
    com.renxuan.blog: info
  file:
    path: log/blog-pro.log
server:
  port: 8081
